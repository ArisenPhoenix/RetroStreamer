# deploy/linux Architecture

`deploy/linux/` owns Linux installation helpers.

Use this directory for Linux-specific install-time actions that are not part of
the main build. Runtime Linux abstractions belong in `src/common/platform/` or
`src/host/hardware/`.
