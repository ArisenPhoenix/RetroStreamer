# src/client

Desktop client implementation lives here.

This directory owns client connection flow, local input capture, media playback,
catalog caching, and desktop platform adapters.

Host behavior belongs under `src/host/`; shared behavior belongs under
`src/common/`.
