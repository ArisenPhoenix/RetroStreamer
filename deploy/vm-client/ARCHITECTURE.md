# deploy/vm-client Architecture

`deploy/vm-client/` owns VM-based desktop client test setup.

The scripts here configure a guest environment for visible media/input testing
without requiring a second physical PC.
