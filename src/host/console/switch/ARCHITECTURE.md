# src/host/console/switch Architecture

`src/host/console/switch/` implements Nintendo Switch-specific host behavior.

Switch-only emulator configuration, update/DLC preparation, save sharing, and
profile handling belong here. Generic stream and session rules should remain in
their owning host buckets.
