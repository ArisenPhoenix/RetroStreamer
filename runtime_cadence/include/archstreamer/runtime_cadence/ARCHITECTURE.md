# runtime_cadence Public API Architecture

This directory defines the public Cadence sidecar API.

It owns sidecar protocol types, store interfaces, DB/file store contracts,
instance operations, and user auth contracts consumed by the sidecar and callers.
