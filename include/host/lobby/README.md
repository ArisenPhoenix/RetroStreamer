# include/host/lobby

Lobby coordination headers live here.

This directory owns pre-launch client gathering, concurrent lobby management,
session hub behavior, and helpers that decide when a join request can become a
session participant.

Active gameplay runtime state belongs under `include/host/session/`.
