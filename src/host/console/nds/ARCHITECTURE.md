# src/host/console/nds Architecture

`src/host/console/nds/` implements Nintendo DS-specific host behavior.

DS-only display layout, touch handling, and emulator launch details belong here.
Reusable touch or input concepts should be lifted out when another console needs
them.
