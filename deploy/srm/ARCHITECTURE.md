# deploy/srm Architecture

`deploy/srm/` owns Steam ROM Manager deployment notes and support files.

This directory documents and supports external catalog/art integration. Catalog
logic itself belongs in `src/common/` and `src/host/console/`.
