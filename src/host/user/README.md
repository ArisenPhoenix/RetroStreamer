# src/host/user

User identity implementation lives here.

This directory implements user credential behavior and user-facing sync helpers.
Database store implementations belong under `src/host/db/`.
