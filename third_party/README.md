# third_party

Third-party source lives here.

Use this directory for vendored or embedded external projects that are kept in
the repository. Generated dependency downloads belong under `_dependencies/` and
should not be treated as source layout.
