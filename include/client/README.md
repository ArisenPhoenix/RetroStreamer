# include/client

Desktop client headers live here.

This area owns client-side session connection, catalog caching, input capture,
media playback, and platform adapters used by the desktop client.

Shared protocol or data model types should go in `include/common/`.
