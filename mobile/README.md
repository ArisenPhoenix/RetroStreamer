# mobile

Mobile clients live here.

The Android project is under `android/`. Mobile-specific UI, input handling,
QR sync, and platform integration should stay in that project unless the logic is
shared with desktop or host code.
