# Android net Package Architecture

`net/` owns Android host connectivity.

This package handles discovery, control connections, remote host helpers, art
fetching, and file logging. Protocol packet definitions belong in `protocol/`.
