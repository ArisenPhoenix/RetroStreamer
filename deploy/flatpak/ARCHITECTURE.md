# deploy/flatpak Architecture

`deploy/flatpak/` owns Flatpak packaging metadata.

The manifest, desktop entry, and metainfo files describe how ArchStreamer is
built and presented as a Flatpak. Runtime source behavior should stay outside
this directory.
