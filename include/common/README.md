# include/common

Cross-target headers live here.

Use this directory for protocol packets, serialization helpers, normalized input
types, catalog identity, pairing data, and other types used by more than one app.

Keep host-only policy in `include/host/` and client-only behavior in
`include/client/`.
