# third_party/melonds Architecture

`third_party/melonds/` contains the vendored melonDS integration source.

Keep project-owned wrappers and generalized emulator orchestration outside this
directory unless a change must patch the vendored code directly.
