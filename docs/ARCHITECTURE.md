# docs Architecture

`docs/` owns durable project knowledge.

Use this directory for behavior contracts, data model notes, control mappings,
and decision records that need to outlive a single code change.

Architecture summaries should link outward to these documents instead of
duplicating detailed behavior.
