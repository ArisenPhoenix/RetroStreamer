# scripts Architecture

`scripts/` owns developer automation and environment helpers.

Scripts here prepare local dependencies, manage catalog support files, configure
host environments, run diagnostics, and wrap repeatable development tasks.

Installer/update behavior belongs in `deploy/`.
