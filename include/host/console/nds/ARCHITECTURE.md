# include/host/console/nds Architecture

`include/host/console/nds/` is for Nintendo DS-specific host contracts.

DS-specific display layout, touch semantics, and emulator launch differences
belong here. Generic touch routing should remain in shared or virtual input
layers if other systems can use it.
