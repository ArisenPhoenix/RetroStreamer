# include/host/user

User identity headers live here.

This directory is for user credentials, user-facing sync behavior, and helpers
that operate on user identity.

Database storage itself belongs under `include/host/db/`.
