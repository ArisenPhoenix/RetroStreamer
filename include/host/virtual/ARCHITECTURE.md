# include/host/virtual Architecture

`include/host/virtual/` defines emulator-visible virtual devices.

This bucket owns virtual gamepads, virtual keyboards, virtual displays, seat
plans, network input receiving, and input routing into emulator devices.

Physical host devices and capture backends belong in `host/hardware/`.
