# include/host/console/switch Architecture

`include/host/console/switch/` is for Nintendo Switch-specific host contracts.

Switch emulator profile handling, update/DLC registration, save sharing, and
future Switch-specific launch policy belong here. Generic session, stream, and
input policy should stay outside this directory.
