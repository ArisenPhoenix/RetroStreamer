# mobile/android/app Architecture

`mobile/android/app/` owns the Android application target.

Gradle builds this module into the installable client. Source code lives under
`src/main/`; tests live under `src/test/`.
