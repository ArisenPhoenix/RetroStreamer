# src/host/hardware

Physical host hardware implementation lives here.

This area implements media capture, media servers, audio sinks, launch
environment discovery, GPU selection, and OS-specific hardware adapters.
