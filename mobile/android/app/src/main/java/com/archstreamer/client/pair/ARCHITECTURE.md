# Android pair Package Architecture

`pair/` owns Android QR and direct/fallback form sync.

It should keep QR payload handling and transfer behavior centralized so phone,
tablet, and TV flows do not drift.
