# scripts/linux Architecture

`scripts/linux/` owns Linux developer and host environment helpers.

These scripts wrap repeatable Linux setup tasks such as firewall rules, Flatpak
builds, GStreamer installation, SRM integration, and emulator BIOS linking.
