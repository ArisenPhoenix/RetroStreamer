# runtime_cadence/src Architecture

`runtime_cadence/src/` implements the Cadence sidecar.

This code owns sidecar storage, protocol handling, instance operations, user
auth, and the sidecar executable entry point.
