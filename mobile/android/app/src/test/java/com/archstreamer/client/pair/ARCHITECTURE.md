# Android pair Tests Architecture

This test package validates Android pairing and QR sync behavior.

Tests here should protect payload parsing, direct transfer, and fallback behavior
from drifting across devices.
