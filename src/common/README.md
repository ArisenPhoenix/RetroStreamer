# src/common

Shared C++ implementation lives here.

Use this directory for code that backs `include/common/`: protocol
serialization, normalized controls, discovery, pairing, hashes, catalog helpers,
and other cross-target utilities.
